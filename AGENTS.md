# AGENTS.md

## Cursor Cloud specific instructions

This repository is currently a bare/minimal repo with only a `README.md` placeholder. There are no application services, dependencies, build systems, or test frameworks configured.

### Current state

- **Language/Framework:** None configured
- **Package manager:** None configured
- **Services:** None
- **Tests:** None
- **Lint:** None
- **Build:** None

### Notes for future agents

- If application code is added in the future, update this file with relevant setup, build, lint, test, and run instructions.
- The update script is a no-op (`echo "No dependencies to install"`) since there are no dependencies. Replace it once a package manager or dependency manifest is introduced.
- Other remote branches (e.g. `sunset-image-*`, `high-jump-technique-*`) contain SVG artwork experiments unrelated to the main branch.
